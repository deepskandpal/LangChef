# Tracker

**Updated 23 August 2026** · `main` at M1–M4 · CI green · 155 tests · 9/9 verify steps

The point of this file is that work can be picked up cold. Every open item says
what it is, why it matters, and which file to open first. Closed items stay for
one milestone and then move to `DECISIONS.md` if they settled something, or are
deleted if they did not.

---

## State of the build

| | Milestone | Status | What exists |
|---|---|---|---|
| M0 | Ground | **done** | decisions, contract, toolchain, CI |
| M1 | Calibration math | **done** | κ + interval, TPR/TNR/PPV/NPV + Wilson, MCC, taxonomy, label planning |
| M2 | Judge runner | **done** | hashed rubrics, provider shim ×3, content-addressed cache, two tiers, pins |
| M3 | Workspace | **done** | `init`, formats, runs, ledger, paired `compare`, memos |
| M4 | Agent layer | **done** | Claude Code skill + commands, gate one enforced at exit 2 |
| M5 | Unattended | next | scheduling, weekly recalibration, spend caps, connectors + sampling |
| M6 | Job one | not started | eval suites, `calibrate diff`, triage, variance-derived thresholds |
| M7 | Experiments | not started | pre-registration, integrity checks, gated readout, `power` |

13 of 19 contracted commands are live. `langchef contract` is the authority;
the table in `src/langchef/core/contract.py` is where a command's status changes.

---

## Open — needs a person

**Reserve `langchef` on PyPI.** Outward-facing and needs the account, so Claude
cannot do it. Exact commands are in `docs/RESERVE-NAMES.md`. The GitHub name is
already taken by this repository. *Blocks: any `pip install langchef` story.*

**Turn on GitHub Pages** for `main` → `/docs` if it is not already serving.
Settings → Pages → Source: Deploy from a branch → `main` / `/docs`. The site is
generated by `scripts/build_docs.py` and committed, so there is no build step to
configure. *Blocks: the docs links in `README.md` resolving.*

---

## Open — decisions pending

**Exit code 1 is doing two jobs.** The contract calls it *"unexpected error"*,
but it is also what comes back for anticipated preconditions — calibrating
before labels exist, an invalid `--kind`, running outside a workspace. 30 call
sites use it against 2 for the specific codes. An agent reading the contract
cannot tell "you have more to do first" from "something broke".
*Options:* add a sixth code for an unmet precondition (adding is safe, the enum
is only documented as never **re**numbered), or reword `1` to plain "error" and
let the message carry it. *Start at:* `src/langchef/core/exits.py`.
*Settle before other people's automation depends on the codes.*

**Exit codes 3 and 4 are advertised and never emitted.** `langchef contract`
promises `3 = abstained` and `4 = budget exhausted`; both have zero call sites.
The command table already models this honestly with `"implemented": false` — the
exit-code table does not. *Options:* mark them reserved in the contract output,
or land them with M5, which is where spend caps live anyway.
*Start at:* `src/langchef/core/contract.py`.

**Revisit the open-core split.** `DECISIONS.md` #8 put the CLI under Apache-2.0
and kept expertise packs proprietary. That was designed around paid pilots, and
#10 replaced pilots with open adoption. The module boundary should stay either
way — it is cheap insurance and `tests/test_boundaries.py` enforces it — but the
licensing conclusion deserves a fresh look before packs have any content.

---

## Open — engineering

**Stratified labels are not yet reweighted.** `label plan` samples stratified by
the judge's own verdict and records an inclusion `weight` per row, but
`calibrate report` computes unweighted sample rates. For balanced strata that is
fine; for a heavily skewed suite the reported TPR/FPR are sample rates, not
population rates. *Fix:* post-stratified estimation using the known stratum
sizes — clean and exact, roughly 80 lines. *Start at:*
`src/langchef/core/sampling.py` (the docstring states the gap) and
`src/langchef/core/agreement.py`.

**The litellm path has never executed.** `LiteLLMProvider.judge` and its
recording branch are marked `pragma: no cover` because there is no key on the
build machine. The deterministic and replay backends are fully covered; the real
one is not. *Fix:* one recorded session against a real model, cassettes
committed, then the replay backend covers it forever. *Start at:*
`src/langchef/judge/providers.py`. *Needs: an API key and about $1.*

**No connectors.** `src/langchef/connect/` is an empty package. M5 wants
DuckDB-first read-only readers plus `langchef sample` for stratified pulls from
production traces. Nothing depends on this yet — goldens are hand-supplied.

**`calibrate diff` is contracted but unbuilt.** Re-score a revised rubric
against the same labels and report the delta. This is the command that makes
rubric iteration cheap, and it is currently the strongest M6 candidate to pull
forward — `compare` was pulled forward the same way for the same reason.

---

## Known limits, deliberately accepted

- **The dogfood's "human" labels are ground truth the harness planted**, not
  people. That is what makes the self-test repeatable, and it is stated in
  `dogfood/README.md`. It is not a substitute for a real labelling study.
- **The default judge is token overlap.** It is a real technique and a real
  starting point, and it has a real blind spot — paraphrase — which calibration
  surfaces on purpose. It is not a good judge and does not claim to be.
- **The bootstrap in `compare` is seeded** (`BOOTSTRAP_SEED = 0`). The contract
  calls `compare` deterministic and this is how that is true.
- **Version pinning of the containment judge is manual.** Changing its checks
  means bumping `VERSION` in `providers.py`, because the model string is part of
  the cache key. There is no test that catches forgetting.

---

## Routine

```sh
./scripts/verify.sh                        # 9 steps; the whole check
uv run pytest tests/test_dogfood.py -v     # the self-test behind the claims
uv run python scripts/build_docs.py        # regenerate docs/ after editing the site
uv run python scripts/render_contract.py   # regenerate the agent contract
```

`verify.sh` runs both generators in `--check` mode, so a stale contract or a
stale docs site fails CI rather than shipping.
